#pragma once

/*
// Objects
extern clFwManager * FirewallManager;
extern bool bFirewallEnabledCOM;

// Functions
NET_FW_IP_VERSION IntToIPVersion(int Integer);
NET_FW_SCOPE IntToScope(int Integer);
NET_FW_IP_PROTOCOL IntToProtocol(int Integer);

// Definitions
#define ERROR_IF_FW_NOT_STARTED		_ErrorException((FirewallManager == NULL),"executing. mnStartFirewall must be used before any other firewall commands",0,__LINE__,__FILE__);

*/